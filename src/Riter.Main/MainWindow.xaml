<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:Riter.Main.Core"
    x:Class="Riter.Main.MainWindow"
    mc:Ignorable="d" ResizeMode="NoResize" UseLayoutRounding="True" 
    WindowStartupLocation="CenterScreen"
    WindowState="Maximized"
    Background="{StaticResource NoneTransparent}" 
    AllowsTransparency="True" 
    WindowStyle="None">
    <Window.DataContext>
        <local:PalleteStateViewModel />
    </Window.DataContext>
    <Grid x:Name="Layout">
        <InkCanvas x:Name="MainInkCanvas" Background="{x:Null}" MoveEnabled="False" ResizeEnabled="False" Cursor="Pen" Margin="0,0,0,-0.333" EditingModeInverted="EraseByPoint">
            <InkCanvas.DefaultDrawingAttributes>
                <DrawingAttributes FitToCurve="True" IgnorePressure="False" IsHighlighter="False" StylusTip="Ellipse" StylusTipTransform="Identity"/>
            </InkCanvas.DefaultDrawingAttributes>
        </InkCanvas>
        <Canvas>
            <StackPanel Orientation="Horizontal"
                         HorizontalAlignment="Center" MouseDown="Palette_MouseDown"
                         MouseLeave="Palette_MouseLeave"
                         MouseUp="Palette_MouseUp"
                         MouseMove="Palette_MouseMove" x:Name="MainPallete" Canvas.Top="130" Canvas.Left="130" MinWidth="0">

                <Grid>
                    <Border
                    CornerRadius="16"
                    Background="#FFF"
                    MaxWidth="460"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    BorderThickness="1">
                        <Border.Effect>
                            <DropShadowEffect Opacity="0.12" ShadowDepth="4" BlurRadius="4"/>
                        </Border.Effect>
                        <StackPanel>
                            <Border Padding="12,12">
                                <DockPanel Margin="0,0,0,0">
                                    <StackPanel  Orientation="Horizontal" VerticalAlignment="Center">
                                        <Border Padding="0,0,0,0" Height="36" CornerRadius="4">
                                            <DockPanel>
                                                <Border Margin="0,0,0,0" Width="32" Height="32" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                    <ContentControl Content="{StaticResource Logo}" />
                                                </Border>
                                            </DockPanel>
                                        </Border>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Border Margin="0,0,4,0"   Width="32" Height="32" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <Button Style="{StaticResource IconButtonStyle}" Content="{StaticResource Icon_Release}" Click="ReleaseButton_Click"/>
                                        </Border>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                            <Border Padding="12,12" Background="#F9F9F9" CornerRadius="8,8,16,16">
                                <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="Center"
                                        Margin="0,0,0,0">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                                        <Border Background="#97D5FF" Margin="0,0,2,0" Width="32" Height="32" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <Button x:Name="DrawButton" Click="DrawButton_Click" Style="{StaticResource IconButtonStyle}" Content="{StaticResource Icon_Pen}" />
                                        </Border>
                                        <Border Width="12" Height="32" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <ContentControl Content="{StaticResource Icon_PenArrow}" />
                                        </Border>

                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                                        <Border Margin="0,0,2,0" Width="32" Height="32" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <Button Style="{StaticResource IconButtonStyle}" Content="{StaticResource Icon_Brush}" />
                                        </Border>
                                        <Border Width="12" Height="32" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <ContentControl Content="{StaticResource Icon_BrushArrow}" />
                                        </Border>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                                        <Border Margin="0,0,2,0"
                                            Width="32" Height="32"
                                            CornerRadius="4"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center">

                                            <Button Style="{StaticResource IconButtonStyle}"
                   Content="{StaticResource Icon_RoundedRectangle}" />
                                        </Border>
                                        <Border Width="12" Height="32" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <ContentControl Content="{StaticResource Icon_SquareArrow}" />
                                        </Border>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                                        <Border Width="32" Height="32" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <Button Style="{StaticResource IconButtonStyle}"
                   Content="{StaticResource Icon_Text}" />
                                        </Border>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                                        <Border Width="32" Height="32" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <Button Style="{StaticResource IconButtonStyle}"
                   Content="{StaticResource Icon_Undo}" />
                                        </Border>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                                        <Border Width="32" Height="32" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <Button Style="{StaticResource IconButtonStyle}"
                   Content="{StaticResource Icon_Redo}" />
                                        </Border>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                                        <Border Width="32"
                                                Height="32"
                                                CornerRadius="4"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center">

                                            <Button Click="EraserButton_Click" Style="{StaticResource IconButtonStyle}"  Content="{StaticResource Icon_Eraser}" />
                                        </Border>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                                        <Border Width="32" Height="32" CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <Button Click="TrashButton_Click" Style="{StaticResource IconButtonStyle}"  Content="{StaticResource Icon_Delete}" />
                                        </Border>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Border>
                </Grid>
                <Grid Margin="4,0,0,0" >
                    <Border MinWidth="48"
                             CornerRadius="16"
                             Background="#FFF"
                             HorizontalAlignment="Center"
                             VerticalAlignment="Center"
                             BorderThickness="1" Height="118" Padding="0,12">

                        <!--#region NewRegion -->
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                            <Border  CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Button ToolTip="Close" Style="{StaticResource IconButtonStyle}" Content="{StaticResource Icon_Close}" Click="ExitButton_Click" />
                            </Border>
                            <Border 
                                    CornerRadius="4"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center">

                                <Button ToolTip="Minimize"
                                           Style="{StaticResource IconButtonStyle}"
                                           Content="{StaticResource Icon_Minimize}"
                                           Click="MinimizeButton_Click"/>
                            </Border>
                            <Border CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Button Style="{StaticResource IconButtonStyle}" Content="{StaticResource Icon_Setting}" ToolTip="Settings" />
                            </Border>
                        </StackPanel>
                        <!--#endregion-->
                    </Border>
                </Grid>
            </StackPanel>
        </Canvas>
    </Grid>
</Window>
