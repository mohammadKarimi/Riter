<UserControl x:Class="Riter.Core.UI.WindowControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="4,0,0,0">
        <Border MinWidth="48" CornerRadius="16" Background="#FFF" HorizontalAlignment="Center" VerticalAlignment="Center" BorderThickness="1" Height="118">
            <Border.Effect>
                <DropShadowEffect Opacity="0.1" ShadowDepth="1" BlurRadius="16"/>
            </Border.Effect>
            <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                <Border CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button ToolTip="Close" Style="{StaticResource IconButtonStyle}" Content="{StaticResource Icon_Close}" Click="ExitButton_Click" />
                </Border>
                <Border CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button ToolTip="Minimize" Style="{StaticResource IconButtonStyle}" Content="{StaticResource Icon_Minimize}" Click="MinimizeButton_Click"/>
                </Border>
                <Border CornerRadius="4" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button Style="{StaticResource IconButtonStyle}" Content="{StaticResource Icon_Setting}" ToolTip="Settings" />
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
